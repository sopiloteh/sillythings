#include "vectors.h"

/**
 * this is a recreation of the vectors.cpp file
 * all it will do is allow user input into a set number of digits
 * into a vector. Once that input is reached it will end and spit out
 * any duplecates that were inserted
 * 
 */

int main()
{
   vector2();
   return 0;
}